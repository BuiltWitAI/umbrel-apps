version: "3.7"

services:

  app_proxy:
    environment:
      APP_HOST: convert
      APP_PORT: 80
      PROXY_AUTH_ADD: "false"

  convert:
    image: ghcr.io/p2r3/convert:latest
    restart: on-failure