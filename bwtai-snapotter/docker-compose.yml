version: "3.7"

services:

  app_proxy:
    environment:
      APP_HOST: bwtai-snapotter_snapotter_1
      APP_PORT: 1349
      PROXY_AUTH_ADD: "false"

  snapotter:
    image: snapotter/snapotter:latest
    user: "1000:1000"
    init: true
    environment:
      - AUTH_ENABLED=true
      - DEFAULT_USERNAME=admin
      - DEFAULT_PASSWORD=admin
      - SKIP_MUST_CHANGE_PASSWORD=false
      - DATABASE_URL=postgres://snapotter:moneyprintergobrrr@bwtai-snapotter_postgres_1:5432/snapotter
      - REDIS_URL=redis://:moneyprintergobrrr@bwtai-snapotter_redis_1:6379
    volumes:
      - ${APP_DATA_DIR}/data/snapotter:/data
      - ${APP_DATA_DIR}/data/workspace:/tmp/workspace
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: on-failure
    healthcheck:
      test: ["CMD", "curl", "-sf", "--max-time", "5", "http://localhost:1349/api/v1/health"]
      interval: 30s
      timeout: 5s
      start_period: 60s
      retries: 3

  postgres:
    image: postgres:17-alpine@sha256:742f40ea20b9ff2ff31db5458d127452988a2164df9e17441e191f3b72252193
    user: "1000:1000"
    environment:
      POSTGRES_USER: snapotter
      POSTGRES_PASSWORD: moneyprintergobrrr
      POSTGRES_DB: snapotter
    volumes:
      - ${APP_DATA_DIR}/data/postgres:/var/lib/postgresql/data
    restart: on-failure
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U snapotter -d snapotter"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 15s

  redis:
    image: redis:8-alpine@sha256:9d317178eceac8454a2284a9e6df2466b93c745529947f0cd42a0fa9609d7005
    user: "1000:1000"
    command: >-
      redis-server
      --maxmemory-policy noeviction
      --maxmemory 512mb
      --appendonly yes
      --requirepass moneyprintergobrrr
    volumes:
      - ${APP_DATA_DIR}/data/redis:/data
    restart: on-failure
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "moneyprintergobrrr", "--no-auth-warning", "ping"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s