version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: all-is-cubes
      APP_PORT: 80
      PROXY_AUTH_ADD: "false"

  all-is-cubes:
    image: ghcr.io/builtwitai/bwtai-all-is-cubes:latest
    restart: on-failure