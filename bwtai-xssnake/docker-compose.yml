version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: xssnake
      APP_PORT: 8002
      PROXY_AUTH_ADD: "false"

  xssnake:
    image: blaiseio/xssnake:latest
    restart: on-failure
    environment:
      - NODE_ENV=production
    ports:
      - "8001:8001"