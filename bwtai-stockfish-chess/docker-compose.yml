version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: stockfish-chess
      APP_PORT: 80
      PROXY_AUTH_ADD: "false"

  stockfish-chess:
    image: ghcr.io/builtwitai/bwtai-stockfish-chess:latest
    restart: on-failure